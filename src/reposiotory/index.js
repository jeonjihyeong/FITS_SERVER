const anonymousReposiotory = require('./anonymous')
const noteRepo = require('./note')
const commentRepo = require('./comment')
const userRepo = require('./user')

module.exports= {
    anonymousReposiotory,noteRepo,commentRepo,userRepo
}